int binary_to_decimal(string str)
		{
		    // Code here.
		    int power = 1;
		    int ans = 0;
		    for(int i = str.length() - 1; i >= 0; i--)
		    {
		        ans += ((str[i] - '0') * power);
		        power *= 2;
		    }
		    return ans;
		}
    tc o(n) sc o(1)
